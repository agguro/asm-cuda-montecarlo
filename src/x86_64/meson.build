# /src/x86_64/meson.build

# Descend into libraries first (if engine or tools depend on them)
subdir('libs')

# Build the data ingestion tools
subdir('tools')

# Build the main GPU simulation engine
subdir('engine')